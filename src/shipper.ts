import { Env, LogShipperFunction } from ".";
import { LogPayload } from "./types";

export const logShipper: LogShipperFunction = async (logPayload: LogPayload[], env: Env) => {
  // Your code goes here
}
